import styled from 'styled-components'

export const StyledFooter = styled.footer`
  display: grid;
  font-size: 0.75rem;
  grid-gap: 0.25rem;
  padding: 1rem 0;
  text-align: center;

  p {
    margin: 0 1rem;
  }
`
